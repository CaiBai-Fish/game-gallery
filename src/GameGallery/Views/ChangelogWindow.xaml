<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GameGallery.Views.ChangelogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="24,18,24,10"
            Spacing="6">
            <TextBlock Style="{ThemeResource TitleTextBlockStyle}" Text="更新日志" />
            <TextBlock
                x:Name="StatusText"
                AutomationProperties.AutomationId="ChangelogStatus"
                FontSize="12"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />
        </StackPanel>

        <ScrollViewer
            x:Name="Scroller"
            Grid.Row="1"
            Padding="24,0,24,16"
            HorizontalScrollMode="Disabled"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <StackPanel x:Name="DocPanel" AutomationProperties.AutomationId="ChangelogDocument" />
        </ScrollViewer>

        <Border
            Grid.Row="2"
            Padding="24,12,24,14"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button
                    AutomationProperties.AutomationId="ChangelogOpenGitHub"
                    Click="OnOpenOnGitHubClick"
                    Content="在 GitHub 上查看" />
                <Button Click="OnCloseClick" Content="关闭" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
